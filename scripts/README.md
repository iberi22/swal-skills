# Scripts — Xavier Operational Scripts

These scripts help Xavier agents bootstrap and operate in new environments.

| Script | Purpose |
|--------|---------|
| `nixos-bootstrap.sh` | First-run setup for NixOS: install deps, connect to Neon/Supabase, pull skills |

All scripts are POSIX-compatible (bash/sh). Run with: `bash scripts/<name>.sh`
